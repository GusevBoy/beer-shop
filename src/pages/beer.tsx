import BeerConteiner from '../components/Beer/BeerConteiner'

type BeerProps = {}

const Beer = (props: BeerProps) => {
  return (
    <div>
			<BeerConteiner />
  	</div>
  );
}

export default Beer;
